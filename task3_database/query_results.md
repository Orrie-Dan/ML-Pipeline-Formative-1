# Task 3 — Query Results

This file shows the required **3+ queries per database** and their **actual output**.
The two sections below are filled in automatically when you run:

```bash
python sql_queries.py      # fills the MySQL section
python mongo_queries.py    # fills the MongoDB section
```

Until you run them against your live databases, the sections show a placeholder.
(You can also paste screenshots of MySQL Workbench / MongoDB Compass here as extra evidence.)

---

## MySQL (relational)

<!--SQL_RESULTS-->
_Not generated yet — run `python sql_queries.py` after loading data with `sql_loader.py`._
<!--/SQL_RESULTS-->

---

## MongoDB (non-relational)

<!--MONGO_RESULTS-->
_Not generated yet — run `python mongo_queries.py` after loading data with `mongo_loader.py`._
<!--/MONGO_RESULTS-->
